# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

#### Классы моделей

#### Класс Products

Класс предназначен для хранения и управления списком товаров.

Конструктор: `products: IProduct[]` — начальный список товаров (по умолчанию пустой массив).

Поля класса:

`products: IProduct[] ` — массив товаров (приватное поле).

`selectedProduct: IProduct | null `— выбранный товар (приватное поле, может быть null).

Методы класса:

`setProducts(products: IProduct[]): void` — устанавливает список товаров.

`getProducts(): IProduct[]` — возвращает список всех товаров.

`getProductById(id: string): IProduct | undefined `— возвращает товар по его идентификатору (id), если товар не найден — возвращает undefined.

`setSelectedProduct(product: IProduct): void `— устанавливает выбранный товар.

`clearSelectedProduct(): void `— сбрасывает выбранный товар (устанавливает selectedProduct в null).

#### Класс Basket

Модель корзины пользователя. Управляет добавлением, удалением и подсчётом товаров.

Конструктор: `events: IEvents` — экземпляр системы событий. Cохраняет переданный экземпляр событий в поле `this.events`

Поля класса:

`items: IProduct[]` — массив товаров в корзине (приватное поле, инициализируется пустым массивом).

Методы класса:

`getItems(): IProduct[]` - возвращает список всех товаров в корзине.

`addItem(product: IProduct): void` - добавляет товар в корзину.

`product: IProduct` — товар, который нужно добавить.

`removeItem(productId: string): void` - удаляет товар из корзины по его идентификатору.

`productId: string` — идентификатор товара для удаления.

`clear(): void `- очищает корзину (удаляет все товары).

`getTotalPrice(): number` - рассчитывает и возвращает общую стоимость всех товаров в корзине.

`getItemCount(): number`- возвращает количество товаров в корзине (число позиций, а не суммарное количество единиц).

`hasItem(productId: string): boolean` - проверяет, есть ли товар с указанным идентификатором в корзине.

`productId: string` — идентификатор товара.

#### Класс Buyer

Хранит и валидирует данные покупателя. Генерирует события при изменении полей.

Конструктор: `events: IEvents` — экземпляр системы событий. сохраняет events и инициализирует `payment = ''`.

Поля класса:

`payment: TPayment | null` — данные способа оплаты (может быть null).

`email: string `— электронная почта покупателя.

`phone: string` — номер телефона покупателя.

`address: string` — адрес доставки.

Методы класса:

`setPayment(payment: TPayment): void` устанавливает способ оплаты.

`setEmail(email: string): void` - устанавливает электронную почту покупателя.

`setPhone(phone: string): void` - устанавливает номер телефона покупателя.

`setAddress(address: string): void` - устанавливает адрес доставки.

`getData(): IBuyer` - возвращает текущие данные покупателя в виде объекта.

`validate(): Partial<Record<keyof IBuyer, string>>` - выполняет валидацию данных покупателя.

`clear(): void` - сбрасывает все данные покупателя (устанавливает поля в начальное/пустое состояние).

#### Класс Server

Слой взаимодействия с сервером через API.

Конструктор:

`constructor(private api: IApi)` — принимает объект API для взаимодействия с сервером.

Поля класса:

`api: IApi` — экземпляр API (приватное поле).

`order: TOrder `— объект с данными заказа.

Методы класса:

`fetchProducts(): Promise<IProduct[]>` - загружает список товаров с сервера.

`sendOrder(order: TOrder): Promise<{ total: number }>` - отправляет заказ на сервер.

#### Представления (Views)

#### Класс BaseCard

Базовый компонент для отображения карточки товара. Предоставляет унифицированный интерфейс для работы с визуальным представлением продукта в разных контекстах (каталог, предпросмотр, корзина).

Конструктор:

`container: HTMLElement` — корневой DOM‑элемент карточки.

`events: IEvents` — экземпляр системы событий.

Методы класса:

`render(data: IProduct): HTMLElement` - создаёт и возвращает HTML‑элемент карточки товара на основе переданных данных.

`setText(element: HTMLElement, text: string): void` - устанавливает текстовое содержимое для указанного HTML‑элемента.

`setImage(element: HTMLImageElement, src: string, alt?: string): void` - устанавливает атрибуты src и alt для HTML‑элемента <img>.

#### Класс CardForBasket

Специализированный компонент для отображения товара в корзине. Наследует базовую логику от `BaseCard`, добавляя функционал нумерации позиций и кнопки удаления.

Конструктор:

`container: HTMLElement` — корневой элемент DOM для карточки в корзине.

`events: IEvents` — экземпляр системы событий.

Поля класса

`private deleteBtn: HTMLButtonElement` - Кнопка удаления товара из корзины (селектор `.basket__item-delete`).

`indexCard: HTMLElement` - Элемент для отображения порядкового номера позиции в корзине (селектор `.basket__item-index`).

Методы класса:

`set index(index: number)` - устанавливает порядковый номер позиции в корзине.

#### Класс CardForCatalog

Специализированный компонент для отображения товара в каталоге. Наследует базовую логику от `BaseCard`, дополняя её функционалом отображения изображения, категории и обработки выбора товара.

Конструктор:

`container: HTMLElement` — корневой элемент DOM для карточки в каталоге.

`events: IEvents` — экземпляр системы событий.

Поля класса:

`_image: HTMLImageElement` — элемент изображения товара (селектор `.card__image`).

`categoryElement: HTMLElement` — элемент отображения категории (селектор `.card__category`).

Методы класса:

`set image(src: string)` - устанавливает источник изображения товара.

`set productData(product: IProduct)` - заполняет карточку данными товара.

`set altText(value: string)` - устанавливает атрибут alt для изображения.

`set buttonText(value: string)` - устанавливает текст кнопки.

`set categoryValue(categoryKey: keyof typeof categoryMap)` - устанавливает категорию товара и применяет соответствующий CSS‑класс.

#### Класс CardForPreview

Специализированный компонент для предпросмотра товара (например, в модальном окне). Наследует базовую логику от `BaseCard`, добавляя элементы описания, категории, кнопки действия и управление их состоянием.

Поля класса:

`imageElement: HTMLImageElement` — элемент изображения товара (селектор `.card__image`).

`category: HTMLElement` — элемент отображения категории (селектор `.card__category`).

`button: HTMLButtonElement` — кнопка действия (селектор `.card__button`).

`descriptionElement: HTMLElement` — элемент для описания товара (селектор `.card__text`).

Конструктор

`container: HTMLElement` — корневой элемент DOM для предпросмотра.

`events: IEvents` — экземпляр системы событий.

#### Класс Modal

Модальное окно для отображения карточек, корзины и форм.

Конструктор:

`container: HTMLElement` — корневой элемент DOM модального окна.

Поля класса:

`closeBtn: HTMLElement` — кнопка закрытия модального окна (селектор .modal**close).

`contentContainer: HTMLElement` — контейнер для содержимого модального окна (селектор .modal**content).

`_handleEscape: (evt: KeyboardEvent) => void` — обработчик нажатия клавиши Esc для закрытия окна.

Методы класса:

`open(content: HTMLElement): void` - открывает модальное окно с указанным контентом.

`close(): void` - закрывает модальное окно. генерирует событие `modal:close`.

`setContent(content: HTMLElement): void` - обновляет содержимое модального окна без открытия/закрытия.

#### Класс Gallery

Выводит карточки товаров на главной странице.

Контруктор:

`container: HTMLElement` — корневой элемент DOM, в котором будет отображаться галерея.

Методы класса:

`set galleryList(cards: HTMLElement[]): void` - устанавливает список карточек товаров для отображения.

`clear(): void`- очищает содержимое галереи (удаляет все карточки).

#### Класс BasketView

Компонент для отображения корзины покупок: управляет списком товаров, общей ценой, кнопкой оформления заказа и сообщением о пустой корзине. Интегрируется с системой событий для уведомления о действиях пользователя.

События:

`basket:open` — инициируется при открытии корзины;

`basket:listChange` — генерируется при изменении списка товаров (добавление/удаление);

`basket:placeOrder` — срабатывает при переходе к оформлению заказа.

Конструктор:

`container: HTMLElement` — корневой элемент DOM корзины.

`events: IEvents` — экземпляр системы событий.

Поля класса:

`events: IEvents` — экземпляр системы событий для эмиссии событий.

`listContainer: HTMLUListElement` — контейнер списка товаров (селектор .basket**list).

`totalPriceElement: HTMLElement` — элемент отображения общей цены (селектор .basket**price).

`submitButton: HTMLButtonElement` — кнопка оформления заказа (селектор .basket\_\_button).

`emptyMessage: HTMLElement` — сообщение «Корзина пуста» (создаётся динамически).

Методы класса:

`private createEmptyMessageElement(): HTMLElement` - создаёт элемент сообщения «Корзина пуста».

`set basketList(cards: HTMLElement)` - заменяет список товаров в корзине.

`set totalPrice(value: number)` - устанавливает общую цену корзины.

`setEmptyMessage(hasItems: boolean)` - показывает/скрывает сообщение «Корзина пуста».

`toggleSubmitButton(enabled: boolean)` - включает/отключает кнопку оформления заказа.

#### Класс BaseForm

Базовый класс для форм, обеспечивающий управление состоянием, валидацией и взаимодействием с DOM.

Конструктор:

`container: HTMLElement` — корневой элемент DOM формы.

`events: IEvents` — экземпляр системы событий.

Поля класса:

`submitButton: HTMLButtonElement` — кнопка отправки формы (<button type="submit">).

`error: HTMLElement` — контейнер для отображения сообщений об ошибках (селектор `.form__errors`).

`container: HTMLElement` — корневой элемент формы.

Методы

`set errorText(text: string)` - устанавливает текст ошибки в контейнер .form\_\_errors.

`toggleSubmitButton(enabled: boolean): void` - включает/отключает кнопку отправки.

`toggleErrors(value: boolean): void` - показывает/скрывает блок ошибок.

`resetFormState(): void` - сбрасывает состояние формы к начальному.

#### Класс FormOrderView

Первый шаг оформления заказа — выбор способа оплаты и адреса.
Особенности: использует CSS‑модификатор `.button_alt-active` для выделения активной кнопки выбора способа оплаты;

Конструктор:

`container: HTMLElement` — корневой элемент DOM формы.

`events: IEvents` — экземпляр системы событий.

Поля класса:

`cardPayButton: HTMLButtonElement` — кнопка выбора оплаты картой (селектор [name="card"]).

`cashPayButton: HTMLButtonElement` — кнопка выбора оплаты наличными (селектор [name="cash"]).

`address: HTMLInputElement` — поле ввода адреса доставки (селектор [name="address"]).

Методы класса:

`checkIsFormValid(errors: IValidationErrors): boolean` - проверяет валидность формы по переданным ошибкам.

`resetFormState(): void` - сбрасывает состояние формы к начальному.

`togglePaymentButtonStatus(status: TPayment): void` - выделяет активную кнопку способа оплаты.

`clear(): void` - очищает поле ввода адреса.

События:

`payment:changed `— при смене способа оплаты;

`address:changed` — при изменении адреса;

`form:order:submit` — при нажатии кнопки «Далее» (переход ко второму шагу).

#### Класс FormContactsView

Второй шаг оформления заказа — ввод контактных данных.

Поля класса:

`emailInput: HTMLInputElement` — поле ввода email (селектор [name=email]).

`phoneInput: HTMLInputElement` — поле ввода телефона (селектор [name=phone]).

Конструктор:

`container: HTMLElement` — корневой элемент DOM формы.

`events: EventEmitter` — экземпляр системы событий.

Методы

`checkIsFormValid(errors: IValidationErrors): boolean` - проверяет валидность формы по переданным ошибкам.

`resetFormState(): void `- сбрасывает состояние формы к начальному.

#### Класс Header

Компонент шапки интерфейса, отображающий кнопку корзины и счётчик товаров в ней. Обеспечивает взаимодействие с корзиной через систему событий.

Конструктор:

`container: HTMLElement` — корневой элемент DOM шапки.

`events: IEvents` — экземпляр системы событий.

Поля класса:

`basketButton: HTMLElement` — элемент кнопки корзины (селектор `.header__basket`).

`basketCounter: HTMLElement` — элемент счётчика товаров в корзине (селектор `.header__basket-counter`).

Методы класса:

`set counter(value: number)` - устанавливает количество товаров в корзине (отображается в счётчике).

#### Класс Success

Компонент для отображения экрана успешного оформления заказа. Показывает итоговую сумму и кнопку закрытия, обеспечивает отправку события при взаимодействии.

Поля класса:

`_totalPrice: HTMLElement` — элемент для отображения итоговой суммы (селектор `.order-success__description`).

`_button: HTMLButtonElement` — кнопка закрытия экрана успеха (селектор `.order-success__close`).

`_events: IEvents` — экземпляр системы событий для эмиссии событий.

Конструктор:

`container: HTMLElement` — корневой элемент DOM шапки.

`events: IEvents` — экземпляр системы событий.

Методы класса:

`set totalPrice(value: number)` - устанавливает и отображает итоговую сумму заказа.

###### Таблица событий системы

| Событие                | Описание                                |
| ---------------------- | --------------------------------------- |
| `products:change`      | Загрузка списка товаров                 |
| `product:select`       | Выбор товара из каталога                |
| `product:selected:set` | Открытие карточки предпросмотра         |
| `product:submit`       | Добавление / удаление товара из корзины |
| `basket:open`          | Открытие корзины                        |
| `basket:listChange`    | Обновление содержимого корзины          |
| `basket:placeOrder`    | Переход к оформлению заказа             |
| `payment:changed`      | Изменение способа оплаты                |
| `address:changed`      | Изменение адреса                        |
| `form:order:submit`    | Отправка формы оплаты                   |
| `form:contacts:submit` | Отправка контактной формы               |
| `modal:close`          | Закрытие модального окна                |
| `success:click`        | Завершение оформления заказа            |

---

###### Презентер (Presenter)

Презентер — компонент, связывающий модель данных и представление (View). Отвечает за:

- обработку событий от View;
- взаимодействие с моделями данных;
- обновление представлений при изменении данных.

**Основные обязанности:**

1. **Обработка пользовательских действий**

   - реагирует на события из View (`product:select`, `basket:open` и др.);
   - передаёт данные в модели (например, добавляет товар в корзину через `Basket`).

2. **Синхронизация данных**

   - при изменении модели (например, после `products:change`) обновляет соответствующие View;
   - обеспечивает согласованность состояния между компонентами.

3. **Управление потоком данных**

   - координирует переходы между экранами (каталог → корзина → оформление заказа);
   - обрабатывает последовательность шагов (например, `basket:placeOrder` → `FormOrderView`).

4. **Валидация и бизнес‑логика**
   - проверяет данные перед отправкой на сервер (через `Buyer.validate()`);
   - вызывает методы `Server` для взаимодействия с API.

**Пример взаимодействия:**

- При событии `product:select`:

  1. Презентер получает ID товара.
  2. Загружает данные товара из модели `Products`.
  3. Инициирует событие `product:selected:set`, открывая `Modal` с `CardForPreview`.

- При событии `basket:placeOrder`:
  1. Презентер проверяет содержимое корзины (`Basket`).
  2. Передаёт управление `FormOrderView` для выбора способа оплаты.
  3. Подписывается на события `payment:changed`, `address:changed` для обновления данных.
